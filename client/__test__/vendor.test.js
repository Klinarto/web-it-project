import { render } from '@testing-library/react'

describe("Unit testing for Vendor Location", () => {
  test("should render ", () => {
    const 
  });
  test("should be pressible ", () => {
    const 
  });
});
});
